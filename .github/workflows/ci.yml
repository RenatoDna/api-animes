name: CI Pipeline

on:
  push:
    branches: [ main, feature/post-animes ]
  pull_request:
    branches: [ main ]

jobs:
  # 🔹 1. Verificação de estilo de código
  lint:
    name: Verificação de Estilo
    runs-on: ubuntu-latest

    steps:
      - name: Checkout do código
        uses: actions/checkout@v4

      - name: Configurar Node.js (exemplo para JS/TS)
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Instalar dependências
        run: npm ci

      - name: Rodar lint
        run: npm run lint

  # 🔹 2. Verificação de cobertura de testes
  test:
    name: Verificação de Cobertura de Testes
    runs-on: ubuntu-latest
    needs: lint

    steps:
      - name: Checkout do código
        uses: actions/checkout@v4

      - name: Instalar dependências
        run: npm ci

      - name: Executar testes com cobertura
        run: npm run test -- --coverage --coverageThreshold='{"global":{"branches":90,"functions":90,"lines":90,"statements":90}}'
